import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'jfs-framework-new',
    template: `
        <h4>Add New Frameworks</h4> 
    `
})
export class JFSFrameworkNewComponent { }
